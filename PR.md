# Pull requests

Address every CI failure and review comment before merging: fix valid findings,
reply with the reason otherwise, and re-check for comments after each push.
Merge only once required checks pass on the latest commit.

Start replies to code review bots with `the following is 100% ai generated:`
followed by a blank line.
