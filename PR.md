# Pull requests

Address all CI and code review comments before merging. Fix valid findings; reply
with the reason when no change is needed. Check for new comments after updates,
and wait for required checks to pass on the latest commit before merging.

Start automated replies to code review bots with `the following is 100% ai generated:`,
followed by a blank line.
